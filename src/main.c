#include "main.h"
Servomotor Servo1;
int main(void)
{
    Board_Config;
    ADC_Init(ADC1);
    //ServoInit(&Servo1,PDI6225MG_300,TIM5,20);
    //ServoSetRange(&Servo1, 0, 60);
    //SetServoAngle(&Servo1,0);

    //init_PID_Regulator();
    while(1)
    {

    }
}

